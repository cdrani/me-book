%h1 Unmet Friends
- @not_friends_yet.each do |user|
  = link_to profile_avatar_select(user.profile), profile_path(user.user_name)
  = user.user_name
%hr
%h1 Friends
- @friends.each do |user|
  = link_to profile_avatar_select(user.profile), profile_path(user.user_name)
  = user.user_name

%h1 Pending
- @pending_friends.each do |user|
  = link_to profile_avatar_select(user.profile), profile_path(user.user_name)
  = user.user_name

%h1 Requested
- @requested_friends.each do |user|
  = link_to profile_avatar_select(user.profile), profile_path(user.user_name)
  = user.user_name

